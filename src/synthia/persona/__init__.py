"""Personas: who SYNTHIA is in a conversation, kept as data rather than code.

A persona is a name, a description, five trait sliders, a few principles and a
prompt template. The system prompt is rendered from them, so changing how
SYNTHIA speaks is editing a file or moving a slider, never a code change.
"""
